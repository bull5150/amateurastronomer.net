import { BlogsearchPipe } from './blogsearch.pipe';

describe('BlogsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BlogsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
